// import { Form } from "./types";

// export function loadForms(): Form[] {
//   return forms;
// }

// export async function loadForm(id: string): Promise<Form> {
//   const form = forms.find((form) => form.id === `form-${id}`);
//   if (!form) {
//     throw new Error(`Form with id "${id}" not found`);
//   }
//   return form;
// }
